#include <stdio.h>
#include <string.h>
int main()
{
	int n;
	scanf("%d", &n);
	int ans[n], guess[n];
	for (int i = 0; i < n; i++)
	{
		scanf("%s", ans);
	}
	for (int i = 0; i < n; i++)
	{
		scanf("%s", guess);
	}
	return 0;
}
