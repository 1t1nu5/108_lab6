#include <stdio.h>
#include <string.h>
int main()
{
	int n;
	scanf("%d", &n);
	int ans[n], guess[n];
	scanf("%s", ans);
	scanf("%s", guess);
	int score;
	for (int i - 0; i < n; i++)
	{
		
	}
	return 0;
}
