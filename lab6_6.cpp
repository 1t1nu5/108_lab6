#include <stdio.h>
#include <string.h>
int main()
{
	int side, pos[2] = {0, 0};
	scanf("%d", side);
	char paper[side][side];
	for (int i = 0; i < side; i++)
	{
		for (int j = 0; j < side; j++)
		{
			paper[side][side] = '.';
		}
	}
	char cmd[250] = {};
	scanf("%s", cmd);
	for (int i = 0; i < 250; i++)
	{
		
	}
	return 0;
}
