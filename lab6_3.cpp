#include <stdio.h>
int main()
{
	int N, Q;
	scanf("%d %d", &N, &Q);
	int a[N];
	char cmd;
	int ncmd[2];
	return 0;
	for (int i = 0; i < N; i++)
	{
		scanf("%c %d %d", &cmd, &ncmd[0], &ncmd[1]);
		if (cmd == 'U')
		{
			
		}
	}
}
